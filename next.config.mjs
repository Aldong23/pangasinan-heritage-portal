/** @type {import('next').NextConfig} */
const nextConfig = {
  reactCompiler: true,
  output: "export",
  basePath: "/pangasinan-heritage-portal",
  assetPrefix: "/pangasinan-heritage-portal/"
};

export default nextConfig;